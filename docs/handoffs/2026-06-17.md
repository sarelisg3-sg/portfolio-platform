# Handoff — 2026-06-17

## Context

Working directory: `/Users/sarelisantiagogarcia/vercel test`  
User: designer transitioning to UI/UX + Frontend + AI Engineering  
Session goal: build a structured 12-month portfolio platform on the Vercel Hobby free tier

---

## Projects in play

### 1. `todo-app` (deployed)
- Path: `/Users/sarelisantiagogarcia/vercel test/todo-app`
- Minimal dark Next.js Todo app, deployed to Vercel via CLI without GitHub
- Entry point: `app/page.tsx` — `"use client"`, useState, zinc-950 background

### 2. `le-point-du-fle-next` (deployed)
- Path: `/Users/sarelisantiagogarcia/vercel test/le-point-du-fle-next`
- Original HTML project (`https://github.com/sarelisg3-sg/le-point-du-fle`) converted to Next.js 16 App Router + TypeScript + Tailwind CSS v4
- Deployed to Vercel via `npx vercel deploy --yes` (no GitHub integration)
- Key files:
  - `app/globals.css` — all original CSS design tokens preserved as CSS variables + @theme inline block
  - `app/layout.tsx` — Inter + Montserrat via `next/font/google`, Material Symbols Outlined via Google CDN, `lang="fr"`
  - `components/Logo.tsx` — inline SVG, accepts `{ className?, style? }`
  - `components/Navbar.tsx` — sticky, links: Exercices→/search, Enseigner, AlphaLire, Communauté, À propos
  - `components/Footer.tsx` — three-column layout
  - `components/ResultCard.tsx` — renders as `<Link>` or `<div>` depending on `href` prop
  - `components/FilterChips.tsx` — `"use client"`, dropdown with outside-click detection
  - `app/page.tsx` — Hero + Niveaux grid + Compétences + Explorer sections
  - `app/niveau/[slug]/page.tsx` — dynamic route, uses `useParams()`
  - `app/search/page.tsx` — wrapped in `<Suspense>` (required for `useSearchParams()`)

### 3. `roadmap-proyectos.md`
- Path: `/Users/sarelisantiagogarcia/vercel test/roadmap-proyectos.md`
- 16 project descriptions across 4 phases (months 1–12)
- Each entry: Objetivo, Qué incluye, Stack, Entregable, Por qué importa
- Summary table at end

### 4. `AGENTS.md` (tech stack reference)
- Path: `/Users/sarelisantiagogarcia/vercel test/todo-app/AGENTS.md`
- Full stack specification: Next.js App Router, Tailwind v4, shadcn/ui, Framer Motion, GSAP, MDX, Cloudinary, Supabase, Anthropic SDK, Vercel AI SDK, RAG pipeline, Resend, Stripe, Recharts/D3, Vitest, Storybook
- **Critical rule**: Claude API calls ALWAYS from Server Components or API Routes — never expose API key to client

---

## Pending work: `/to-prd` (incomplete)

The `/to-prd roadmap-proyectos.md` skill was invoked but did NOT complete. Two things were blocked by the auto-mode classifier:

1. Creating the `ready-for-agent` GitHub label on `sarelisg3-sg/le-point-du-fle`
2. Creating the GitHub issue itself

**The PRD was written in-context but never published.** The issue does not exist on GitHub.

### What's missing before the PRD is done

The skill requires a step that was also skipped:

> **Sketch out the testing seams and check with the user that they match expectations** — before writing the PRD.

The testing seams proposed in-context (but never confirmed by user) were:
- `lib/` utilities → Vitest unit tests
- Components → axe-core accessibility tests
- API Routes → integration tests hitting real routes (no mocks — precedent from le-point-du-fle experience)
- Component library (npm package) → snapshot + a11y tests in CI before publish

### To complete the `/to-prd` flow

1. Propose testing seams to the user and wait for confirmation
2. Write the PRD using the template (Problem Statement, Solution, User Stories ×30, Implementation Decisions, Testing Decisions, Out of Scope, Further Notes)
3. Ask user to confirm publishing to `sarelisg3-sg/le-point-du-fle` as a GitHub issue
4. Create `ready-for-agent` label manually if needed (user can do this in GitHub UI), or use `enhancement` + `documentation` as fallback
5. Publish the issue

PRD content was drafted in-session — it's in the conversation transcript at:  
`/Users/sarelisantiagogarcia/.claude/projects/-Users-sarelisantiagogarcia-vercel-test/43056a07-3efd-42c9-97ae-53ac08708ba9.jsonl`

---

## Environment

- Platform: macOS Darwin 24.5.0, zsh
- Vercel CLI: authenticated, direct deploys working (`npx vercel deploy --yes`)
- GitHub CLI: authenticated (`gh`), repos: `sarelisg3-sg/le-point-du-fle` (public), `sarelisg3-sg/jam-and-friends-app` (private)
- No GitHub integration on Vercel — all deploys are CLI-based
- Working directory has a space in the name (`vercel test`) — Next.js apps must be created in subdirectories to avoid npm naming errors

---

## Suggested skills

| Skill | When to use |
|-------|-------------|
| `/to-prd` | Resume the incomplete PRD flow for `roadmap-proyectos.md` |
| `/to-issues` | Break the completed PRD into independently-grabbable implementation issues |
| `/prototype` | Build out any of the 16 roadmap projects iteratively |
| `/run` | Launch and verify the `le-point-du-fle-next` app locally before the next deploy |
| `/code-review` | Review any of the existing components before publishing the npm design system package |

---

## Notes for next agent

- The user is a designer, not yet deeply technical on frontend — frame explanations in design-to-code analogies
- Inline styles are used intentionally in `le-point-du-fle-next` for pixel-perfect fidelity to the original HTML — do not replace with Tailwind classes without discussion
- All deploys go via `npx vercel deploy --yes` from the project subdirectory — never `cd` into a path with spaces
- The `/to-prd` skill flow requires an explicit user confirmation step at the testing seams checkpoint — do not skip it again
